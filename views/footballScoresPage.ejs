<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Football Scores</title>
    <link rel="icon" type="image/x-icon" href="/images/favicon.png">
</head>
<body>
    <div class="nav">
        <a class="active" href="/home.html">Home</a>
        <a href="/football/football.html">Football</a>
        <a href="/basketball/basketball.html">Basketball</a>
        <a href="/baseball/baseball.html">Baseball</a>
        <a href="/messaging/messages.html">Messages</a>
        <a href="/profile.html">Profile</a>
    </div>
    <div class="footballNav">
        <a class="football" href="/football/aboutFootball.html">About Football</a>
        <a href="/football/scores">Check Scores</a>
        <a href="/football/fbLiveChat/liveChatEntry.html">Live Chat</a>
    </div>
    <ul class="scoresContainer">
        <% for ( const team of teams ) { %>
            <a href="/users/<%= team.name %>" class="team">
                <li>
                    <div class="info">
                        <p>
                            <%= team.teamName %>
                            <% if (team.homeAway === "Home") { %>
                                - <%= team.homeAway; %>
                            <% } else { %>
                                    - Away
                            <% } %>
                            <%= team.teamScore %>
                            <img src="<%= team.teamLogo %>" alt="<%= team.name %> logo"
                            width="25"
                            height="25" />
                        </p>
                        <p> 
                            vs. 
                            <%= teams[team.teamId - 1].teamName %>
                            <% if (teams[team.teamId - 1].homeAway === "Home") { %>
                                - <%= teams[team.teamId - 1].homeAway; %>
                            <% } else { %>
                                    - Away
                            <% } %>
                            <%= teams[team.teamId - 1].teamScore %>
                            <img src="<%= teams[team.teamId - 1].teamLogo %>" alt="<%= teams[team.teamId - 1].name %> logo"
                            width="25"
                            height="25" />
                        </p>
                    </div>
                </li>
            </a>
        <% } %>
    </ul>
</body>
</html>